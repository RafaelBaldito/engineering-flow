# Environment Preflight Implementation

The approved design is implemented by `scripts/env-preflight` and
`tools/env_preflight.py`. The focused tests cover the injected probe failure
classes and the current-checkout launcher interface. No durable deviations from
the approved design were required.
